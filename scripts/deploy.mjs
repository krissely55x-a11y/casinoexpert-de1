#!/usr/bin/env node
/** Cloudflare Pages deploy step — dist/ is published automatically after build. */
console.log('OK: dist/ will be published by Cloudflare Pages (static output).');
process.exit(0);
